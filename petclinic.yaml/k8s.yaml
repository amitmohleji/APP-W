apiVersion: apps/v1
kind: Deployment
metadata:
  name: APP-W-d
  namespace: APP-W
  labels:
    app: APP-W
spec:
  replicas: 1
  selector:
    matchLabels:
      app: APP-W
  template:
    metadata:
      labels:
        app: APP-W
    spec:
      containers:
      - image: amitmohleji/appw:{{BUILD_NUMBER}}
        name: APP-W
---
apiVersion: v1
kind: Service
metadata:
  name: APP-W
  namespace: APP-W
spec:
  selector: 
    app: APP-W
  type: ClusterIP
  ports:
      - port: 8080
---
kind: Route
apiVersion: route.openshift.io/v1
metadata:
  name: APP-W
  namespace: APP-W
spec:
  to:
    kind: Service
    name: APP-W
    weight: 100
  port:
    targetPort: 8080
  wildcardPolicy: None
status:
  ingress:
    - routerName: default
      conditions:
        - type: Admitted
          status: 'True'
      wildcardPolicy: None

